<turbo-frame id="tenant-row-<%= params[:id] %>">
  <%= render "properties/tenants/row",
        tenant: @tenant,
        index: @tenant_index,
        report: @report,
        property: @property %>
</turbo-frame>
