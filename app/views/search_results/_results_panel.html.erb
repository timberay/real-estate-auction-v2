<%# Locals:
      search_results          — relation/array of SearchResult (paginated, may be empty)
      search_page             — Integer (1-based)
      total_pages             — Integer
      api_total_count         — Integer or nil
      over_api_limit          — Boolean
      existing_case_numbers   — Set of case_number strings
%>
<div id="criteria-search-results">
  <% if search_results&.any? %>
    <%= render "search_results/inline_results",
               search_results: search_results,
               search_page: search_page,
               total_pages: total_pages,
               api_total_count: api_total_count,
               over_api_limit: over_api_limit,
               existing_case_numbers: existing_case_numbers %>
  <% elsif api_total_count.to_i.zero? && !api_total_count.nil? %>
    <div class="bg-slate-100 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl p-6 mt-3 text-center">
      <p class="text-sm text-slate-700 dark:text-slate-200 font-medium">조건에 맞는 물건이 없습니다.</p>
      <p class="mt-1 text-xs text-slate-500 dark:text-slate-400">관심 지역이나 최대 입찰가를 조정해 다시 시도해 보세요.</p>
    </div>
  <% end %>
</div>
