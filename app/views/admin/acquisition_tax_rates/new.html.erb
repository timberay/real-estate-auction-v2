<div class="max-w-2xl mx-auto px-4 py-8">
  <header class="mb-6">
    <h1 class="text-2xl font-bold text-slate-900 dark:text-slate-100">취득세율 추가</h1>
    <p class="text-sm text-slate-500 dark:text-slate-400 mt-1">
      세법 개정으로 새 (부동산 유형 × 세대 × 가액) 구간이 생겼을 때 사용합니다.
    </p>
  </header>

  <%= render "form",
        rate: @rate,
        form_url: admin_acquisition_tax_rates_path,
        form_method: :post,
        submit_label: "추가" %>
</div>
